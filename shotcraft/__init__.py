"""shotcraft — a read-only shot record for Meticulous espresso machines.

The version lives here and nowhere else. The banner, `--version`, and package
metadata all read this one constant, so they cannot drift apart and quietly
disagree about what you are running.
"""

__version__ = "0.1.0"
